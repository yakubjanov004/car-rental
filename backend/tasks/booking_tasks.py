"""Booking-related background tasks placeholder."""


def send_booking_reminder(booking_id):
    # Placeholder until Celery worker is introduced.
    return {'status': 'noop', 'booking_id': booking_id}
