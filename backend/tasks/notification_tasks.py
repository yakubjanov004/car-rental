"""Notification-related background tasks placeholder."""


def send_notification_async(user_id, title):
    # Placeholder until message broker is configured.
    return {'status': 'noop', 'user_id': user_id, 'title': title}
